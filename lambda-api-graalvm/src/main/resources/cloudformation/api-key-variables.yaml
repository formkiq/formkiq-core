#@ load("@ytt:overlay", "overlay")

#@overlay/match by=overlay.all
---
Resources:
  DocumentsKeyApi:
    Properties:
      Body:
        info:
          #@overlay/replace
          title: "FormKiQ KEY API"