#@ load("@ytt:overlay", "overlay")

#@overlay/match by=overlay.all
---
Resources:
  DocumentsIamApi:
    Properties:
      Body:
        info:
          #@overlay/replace
          title: "FormKiQ IAM API"