     
Resources:

  CoreStorage:
    Properties:
      Location: lambda-s3/template-docker.yaml

  CoreApi:
    Properties:
      Location: lambda-api/template-docker.yaml
          
  CoreConsole:
    Properties:
      Location: console/template-docker.yaml

  ModuleEmailNotify:
    Properties:
      Location: module-email-notify/template-docker.yaml