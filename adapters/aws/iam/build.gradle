
description = "AWS IAM"

dependencies {
  api libs.aws.sdk.iam
  implementation libs.aws.sdk.url.connection.client

  configurations.all {
    exclude group: 'software.amazon.awssdk', module: 'apache-client'
    exclude group: 'software.amazon.awssdk', module: 'netty-nio-client'
  }
}
